<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.goparamore</groupId>
    <artifactId>Lookup</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>Receiver</artifactId>

  <dependencies>
    <dependency>
      <groupId>io.goparamore</groupId>
      <artifactId>Model</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>io.goparamore</groupId>
      <artifactId>SimpleMessaging</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>io.goparamore</groupId>
      <artifactId>SimpleEventing</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
    <!-- We are the composition root, so we are the module that chooses SQLite. Model declares
         IPriceStore and never hears about this line. -->
    <dependency>
      <groupId>io.goparamore</groupId>
      <artifactId>LocalCopy</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>

    <!-- The gateway declares amqp-client as optional, so it does not reach us on its own.
         We are the composition root: we are the module that chooses the broker, and we need the
         jar on the classpath at run time. 'runtime' scope says exactly that: give me the jar,
         I do not name anything in it. -->
    <dependency>
      <groupId>com.rabbitmq</groupId>
      <artifactId>amqp-client</artifactId>
      <version>${amqp.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.kafka</groupId>
      <artifactId>kafka-clients</artifactId>
      <version>${kafka.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
      <version>${slf4j.version}</version>
      <scope>runtime</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- So the jar is at Receiver/target/Receiver.jar, which is short enough to type and short
         enough for 'pgrep -f Receiver.jar' to find. -->
    <finalName>Receiver</finalName>
    <plugins>
      <!-- An executable jar plus its dependencies in target/lib, rather than one fat jar:
           it builds in a fraction of the time and 'java -jar' works either way. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifest>
              <mainClass>receiver.Receiver</mainClass>
              <addClasspath>true</addClasspath>
              <classpathPrefix>lib/</classpathPrefix>
            </manifest>
          </archive>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>copy-dependencies</id>
            <phase>prepare-package</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <outputDirectory>${project.build.directory}/lib</outputDirectory>
              <includeScope>runtime</includeScope>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
